# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Option 1: Local PostgreSQL Database (Recommended for Development)
# Format: postgresql://username:password@localhost:5432/database_name
# Example: postgresql://postgres:mypassword@localhost:5432/ventus_db
DATABASE_URL=postgresql://user:password@localhost:5432/ventus_db

# Option 2: Render Production Database (External URL for Local Testing)
# Get this from: Render Dashboard → Your Database → External Database URL
# Format: postgresql://user:password@host:port/database
# Note: Make sure to use the EXTERNAL URL, not the internal one
# DATABASE_URL=postgresql://user:password@host:port/database

# Database SSL Configuration (Optional)
# SSL is automatically disabled for localhost connections
# For remote databases, SSL is enabled in production by default
# You can override this behavior by setting DATABASE_SSL=true or DATABASE_SSL=false
# DATABASE_SSL=false

# JWT Secret for token signing (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
NODE_ENV=development

# Port
PORT=5000

# Hotel API proxy (/v2) - used when frontend calls this backend for search/availability/booking
# Optional: only needed if staging frontend uses REACT_APP_API_BASE pointing to this backend
HOTEL_API_BASE=https://api-staging.littleemperors.com
HOTEL_API_TOKEN=your_hotel_api_token_here

# Mailgun transactional email (server only; use the dedicated Ventus domain sending key)
# Dedicated EU domain created and DNS-verified in Vinadamo.
MAILGUN_API_KEY=
MAILGUN_DOMAIN=mg.ventustravel.co.uk
# Must match the region selected when creating the domain: US or EU.
MAILGUN_REGION=EU
MAILGUN_FROM_EMAIL=Ventus Travel <no-reply@mg.ventustravel.co.uk>

# Password reset email delivery
PUBLIC_APP_URL=https://destinations.ventustravel.co.uk
# Optional sender override; defaults to MAILGUN_FROM_EMAIL.
# PASSWORD_RESET_FROM_EMAIL=Ventus Travel <no-reply@mg.ventustravel.co.uk>
# Optional address for replies to password reset emails
PASSWORD_RESET_REPLY_TO=daniella@ventustravel.co.uk

# Optional existing providers, used only when Mailgun is not configured.
# Resend also requires PASSWORD_RESET_FROM_EMAIL.
RESEND_API_KEY=
# Temporary fallback: reuse the existing EmailJS service when neither provider is configured.
# A dedicated password-reset EmailJS template is preferable to the booking template.
EMAILJS_SERVICE_ID=
EMAILJS_TEMPLATE_ID=
EMAILJS_PUBLIC_KEY=

# PayPal Configuration
# Create separate sandbox and live REST apps in the PayPal developer dashboard.
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_WEBHOOK_ID=your_paypal_webhook_id
# Use sandbox locally and live only with live REST app credentials.
PAYPAL_ENVIRONMENT=sandbox

# Optional membership coupons. Keep these on the server only.
# Example: {"WELCOME100":{"discountPercent":100,"description":"Complimentary membership"}}
MEMBERSHIP_COUPONS_JSON={}

# Booking requests are always persisted. Configure this address to also receive notifications.
BOOKING_NOTIFICATION_EMAIL=daniella@ventustravel.co.uk
# Optional; defaults to MAILGUN_FROM_EMAIL, then PASSWORD_RESET_FROM_EMAIL.
# BOOKING_FROM_EMAIL=Ventus Travel <bookings@mg.ventustravel.co.uk>
